---
title: "Dasbor Admin Reaktif & Modern dengan Filament PHP"
courseSlug: "laravel-modern-mastery"
chapter: "Bab 3: Dasbor Admin & API Enterprise"
order: 5
duration: "35 Menit"
contentType: "text"
---

import Callout from '../../components/mdx/Callout.astro';

**Filament PHP** adalah ekosistem panel admin modern berbasis **TALL Stack (Tailwind CSS, Alpine.js, Laravel, Livewire)** yang memungkinkan Anda membangun panel CRUD reaktif, tabel data dengan filter pencarian canggih, form builder dinamis, dan widget analitik dalam hitungan menit tanpa menulis JavaScript kompleks.

---

## 1. Memasang Filament Panel Builder di Laravel

```bash
# Instalasi Filament v3
composer require filament/filament:"^3.2" -W
php artisan filament:install --panels
```

Setelah instalasi selesai, panel admin modern dapat langsung diakses pada URL: `http://localhost:8000/admin`.

---

## 2. Membuat Filament Resource (CRUD Lengkap Otomatis)

Cukup jalankan perintah artisan untuk menggenerate Resource untuk Model `Course`:

```bash
php artisan make:filament-resource Course --generate
```

Perintah ini akan menghasilkan kelas `app/Filament/Resources/CourseResource.php`:

```php
namespace App\Filament\Resources;

use App\Models\Course;
use Filament\Forms;
use Filament\Forms\Form;
use Filament\Resources\Resource;
use Filament\Tables;
use Filament\Tables\Table;

class CourseResource extends Resource
{
    protected static ?string $model = Course::class;
    protected static ?string $navigationIcon = 'heroicon-o-academic-cap';
    protected static ?string $navigationGroup = 'Manajemen Akademik';

    // 1. Skema Formulir Input (Form Builder)
    public static function form(Form $form): Form
    {
        return $form->schema([
            Forms\Components\TextInput::make('title')
                ->required()
                ->maxLength(200)
                ->live(onBlur: true)
                ->afterStateUpdated(fn (Forms\Set $set, ?string $state) => $set('slug', str()->slug($state))),

            Forms\Components\TextInput::make('slug')
                ->required()
                ->unique(ignoreRecord: true),

            Forms\Components\Select::make('category_id')
                ->relationship('category', 'name')
                ->searchable()
                ->preload()
                ->required(),

            Forms\Components\TextInput::make('instructor')
                ->default('I Putu Agus Wahyu Dupayana')
                ->required(),

            Forms\Components\TextInput::make('price')
                ->numeric()
                ->prefix('Rp')
                ->default(0),

            Forms\Components\Toggle::make('is_published')
                ->default(true),

            Forms\Components\RichEditor::make('description')
                ->columnSpanFull(),
        ]);
    }

    // 2. Skema Tabel Data (Table Builder)
    public static function table(Table $table): Table
    {
        return $table
            ->columns([
                Tables\Columns\TextColumn::make('title')
                    ->searchable()
                    ->sortable(),
                Tables\Columns\TextColumn::make('category.name')
                    ->badge()
                    ->color('info'),
                Tables\Columns\TextColumn::make('instructor')
                    ->searchable(),
                Tables\Columns\TextColumn::make('price')
                    ->money('IDR')
                    ->sortable(),
                Tables\Columns\IconColumn::make('is_published')
                    ->boolean(),
                Tables\Columns\TextColumn::make('created_at')
                    ->dateTime()
                    ->toggleable(isToggledHiddenByDefault: true),
            ])
            ->filters([
                Tables\Filters\TernaryFilter::make('is_published'),
                Tables\Filters\SelectFilter::make('category')
                    ->relationship('category', 'name'),
            ])
            ->actions([
                Tables\Actions\EditAction::make(),
                Tables\Actions\DeleteAction::make(),
            ])
            ->bulkActions([
                Tables\Actions\BulkActionGroup::make([
                    Tables\Actions\DeleteBulkAction::make(),
                ]),
            ]);
    }
}
```

---

## 3. Menambahkan Widget Statistik Analitik

Anda dapat menampilkan kartu metrik ringkasan di dashboard Filament:

```php
namespace App\Filament\Widgets;

use App\Models\Course;
use App\Models\User;
use Filament\Widgets\StatsOverviewWidget as BaseWidget;
use Filament\Widgets\StatsOverviewWidget\Stat;

class PlatformStatsOverview extends BaseWidget
{
    protected function getStats(): array
    {
        return [
            Stat::make('Total Siswa Terdaftar', User::where('role', 'student')->count())
                ->description('Peningkatan 18% bulan ini')
                ->descriptionIcon('heroicon-m-arrow-trending-up')
                ->color('success'),

            Stat::make('Kursus Aktif', Course::where('is_published', true)->count())
                ->color('primary'),

            Stat::make('Total Pendaftaran', \App\Models\Enrollment::count())
                ->color('info'),
        ];
    }
}
```

<Callout type="tip" title="Kekuatan Livewire di Filament">
  Setiap interaksi pencarian, filter, dan pagination di tabel Filament berjalan secara asinkron (*AJAX*) via Livewire tanpa perlu me-reload seluruh halaman browser!
</Callout>
